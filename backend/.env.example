# RPC 节点地址 (支持 WebSocket)
# 示例: https://sepolia.infura.io/v3/YOUR_INFURA_PROJECT_ID
RPC_URL=

# Pool 合约地址
# 部署后会自动更新此地址
POOL_CONTRACT_ADDRESS=

# MySQL 数据库配置
MYSQL_DSN=root:password@tcp(127.0.0.1:3306)/dex_pro?charset=utf8mb4&parseTime=True&loc=Local

# 服务器端口
PORT=8080
